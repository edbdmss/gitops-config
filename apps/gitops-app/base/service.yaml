apiVersion: v1
kind: Service
metadata:
  name: spring-boot-svc
  labels:
    app: gitops-app
spec:
  selector:
    app: gitops-app
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  type: ClusterIP